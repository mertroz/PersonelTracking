@model PersonnelTrackingSystem.WebApp.Models.MaterialViewModel

@{
    ViewData["Title"] = "Update";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<h4>Malzeme Bilgilerini Güncelleme</h4>

<div class="row">
    <div class="col-12 col-xl-8">
        @if (TempData.ContainsKey("ResultMessage"))
        {
            <div class="alert alert-danger ">
                @TempData["ResultMessage"]
            </div>
        }
        <form asp-action="Update">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <div class="card card-body border-0 shadow mb-4">
                    <h2 class="h5 mb-4">Malzeme Bilgileri</h2>
                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label asp-for="Employees" class="control-label">Personel Bilgisi</label>
                            <select asp-for="EmployeeId" class="form-select"
                                    asp-items="@(new SelectList(Model.Employees, "Id", "FullName"))">
                                <option>Please select one</option>
                            </select>
                        </div>
                        <div class="col-md-6 mb-3">
                            <label asp-for="Request" class="control-label">İstek</label>
                            <input asp-for="Request" class="form-control" />
                            <span asp-validation-for="Request" class="text-danger"></span>
                        </div>
                    </div>
                    <div class="mt-3">
                        <button class="btn btn-gray-800 mt-2 animate-up-2" type="submit">Kaydet</button>
                    </div>
                </div>
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Listeye Geri Dön</a>
</div>
