@model PersonnelTrackingSystem.WebApp.Models.SalaryPaymentSaveModel

@{
    ViewData["Title"] = "AllMonthPayment";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h4>Toplu Maaş Girişi</h4>

<div class="float-start col-4">
    @if (TempData.ContainsKey("ResultMessage"))
    {
        <div class="alert alert-success">
            @TempData["ResultMessage"]
        </div>
    }
</div>
<div class="row">
    <div class="col-md-4">
        <form asp-action="AllMonthPayment">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Month" class="control-label">Ay</label>
                <select asp-for="Month" class="form-select mb-0" required>
                    <option selected value="">Seçiniz</option>
                    <option value="1">Ocak</option>
                    <option value="2">Şubat</option>
                    <option value="3">Mart</option>
                    <option value="4">Nisan</option>
                    <option value="5">Mayıs</option>
                    <option value="6">Haziran</option>
                    <option value="7">Temmuz</option>
                    <option value="8">Ağustos</option>
                    <option value="9">Eylül</option>
                    <option value="10">Ekim</option>
                    <option value="11">Kasım</option>
                    <option value="12">Aralık</option>
                </select>
                <span asp-validation-for="Month" class="text-danger"></span>
            </div>

             @{
                var thisYear = DateTime.Now.Year;
                var endYear = thisYear + 10;
                var yearList = Enumerable.Range(thisYear, 11).ToList();
            }

            <div class="form-group">
                <label asp-for="Year" class="control-label">Yıl</label>
                <select asp-for="Year" class="form-select mb-0" required>
                    <option selected value="">Seçiniz</option>
                    @foreach (var year in yearList)
                    {
                            <option value="@year">@year</option>
                    }
                </select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Listeye Geri Dön</a>
</div>

